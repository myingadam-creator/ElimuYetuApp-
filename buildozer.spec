   [app]
   title = ElimuYetuApp
   package.name = elimuyetuapp
   package.domain = org.elimuyetu
   source.dir =.
   source.include_exts = py,png,jpg,kv,atlas,json
   version = 0.1
   requirements = python3,kivy
   android.archs = arm64-v8a, armeabi-v7a
   
   [buildozer]
   log_level = 2
   warn_on_root = 1
#test
